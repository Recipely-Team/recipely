/**
 * How many cuisines ride the rail before the rest are left to the sheet.
 *
 * The taxonomy runs to about forty. The rail carries the handful people
 * actually reach for and shows that it continues; completeness lives behind the
 * "all cuisines" button, so neither surface has to grow when the catalogue does.
 */
export const RAIL_CUISINE_COUNT = 10;
