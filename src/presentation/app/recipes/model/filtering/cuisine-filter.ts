/**
 * The pseudo-key the web cuisine strip sends for "no cuisine filter".
 *
 * It is not a cuisine — it is the reset. It lived as a local `ALL_KEY` in the
 * grid and as a bare `'ALL'` in the mutation that answers it, so the two ends
 * of the same protocol were written independently.
 */
export const ALL_CUISINES_KEY = 'ALL';
