/** The slot the file import copy leaves for a count: "Page {n}", "{n} / {max}". */
export const COUNT_TOKEN = '{n}';

/** The slot "{n} / {max}" leaves for the page limit. */
export const MAX_TOKEN = '{max}';
